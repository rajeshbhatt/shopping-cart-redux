// import { fromJS } from 'immutable';
// import { makeSelectCartDomain } from '../selectors';

// const selector = makeSelectCartDomain();

describe('makeSelectCartDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
