// import { fromJS } from 'immutable';
// import { makeSelectCatalogDomain } from '../selectors';

// const selector = makeSelectCatalogDomain();

describe('makeSelectCatalogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
