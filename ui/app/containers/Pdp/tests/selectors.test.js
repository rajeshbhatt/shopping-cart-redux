// import { fromJS } from 'immutable';
// import { makeSelectPdpDomain } from '../selectors';

// const selector = makeSelectPdpDomain();

describe('makeSelectPdpDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
