// import { fromJS } from 'immutable';
// import { makeSelectShortlistDomain } from '../selectors';

// const selector = makeSelectShortlistDomain();

describe('makeSelectShortlistDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
